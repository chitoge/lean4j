<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.lean4j</groupId>
  <artifactId>lean4j</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>lean4j</name>
  <description>Lean 4 on GraalVM — native bridge and Truffle polyglot language</description>

  <properties>
    <maven.compiler.source>25</maven.compiler.source>
    <maven.compiler.target>25</maven.compiler.target>
    <maven.compiler.release>25</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <graalvm.version>24.2.1</graalvm.version>
    <lean.toolchain>${user.home}/.elan/toolchains/leanprover--lean4---v4.31.0</lean.toolchain>
    <lean.bin>${user.home}/.elan/bin/lean</lean.bin>
    <leanc.bin>${user.home}/.elan/bin/leanc</leanc.bin>
    <lean.lib.path>${project.basedir}/lean-runtime/liblean4j.so</lean.lib.path>
  </properties>

  <dependencies>
    <!-- Truffle API for GraalVM polyglot language implementation -->
    <dependency>
      <groupId>org.graalvm.truffle</groupId>
      <artifactId>truffle-api</artifactId>
      <version>${graalvm.version}</version>
    </dependency>
    <dependency>
      <groupId>org.graalvm.polyglot</groupId>
      <artifactId>polyglot</artifactId>
      <version>${graalvm.version}</version>
    </dependency>

    <!-- Test -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.10.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <sourceDirectory>core/src/main/java</sourceDirectory>
    <testSourceDirectory>core/src/test/java</testSourceDirectory>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>25</release>
          <compilerArgs>
            <arg>--enable-preview</arg>
            <arg>-proc:full</arg>
          </compilerArgs>
          <!-- Use GraalVM's javac for annotation processing (Truffle DSL) -->
          <fork>true</fork>
          <executable>/opt/graalvm-community-openjdk-25.0.2+10.1/bin/javac</executable>
          <annotationProcessorPaths>
            <path>
              <groupId>org.graalvm.truffle</groupId>
              <artifactId>truffle-dsl-processor</artifactId>
              <version>${graalvm.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>

      <!-- Build Lean shared library before Java compilation -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.3.0</version>
        <executions>
          <!-- Step 1: compile Lean to C -->
          <execution>
            <id>lean-to-c</id>
            <phase>generate-sources</phase>
            <goals><goal>exec</goal></goals>
            <configuration>
              <executable>bash</executable>
              <arguments>
                <argument>-c</argument>
                <argument>${lean.bin} "--c=${project.basedir}/lean-runtime/Lean4J.c" "${project.basedir}/lean-runtime/Lean4J.lean"</argument>
              </arguments>
            </configuration>
          </execution>
          <!-- Step 2: compile C to shared library -->
          <execution>
            <id>c-to-so</id>
            <phase>generate-sources</phase>
            <goals><goal>exec</goal></goals>
            <configuration>
              <executable>bash</executable>
              <arguments>
                <argument>-c</argument>
                <argument>${leanc.bin} -shared -fPIC -fvisibility=default -I${lean.toolchain}/include ${project.basedir}/lean-runtime/Lean4J.c ${project.basedir}/lean-runtime/lean4j_bridge.c -L${lean.toolchain}/lib/lean -lleanshared -Wl,-rpath,${lean.toolchain}/lib/lean -o ${project.basedir}/lean-runtime/liblean4j.so</argument>
              </arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <argLine>--enable-preview --enable-native-access=ALL-UNNAMED -ea</argLine>
          <systemPropertyVariables>
            <lean4j.lib>${lean.lib.path}</lean4j.lib>
          </systemPropertyVariables>
        </configuration>
      </plugin>

      <!-- Spike runner -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.3.0</version>
        <configuration>
          <mainClass>lean4j.Lean4JSpike</mainClass>
          <executable>/opt/graalvm-community-openjdk-25.0.2+10.1/bin/java</executable>
          <arguments>
            <argument>--enable-preview</argument>
            <argument>--enable-native-access=ALL-UNNAMED</argument>
            <argument>-ea</argument>
            <argument>-Dlean4j.lib=${lean.lib.path}</argument>
            <argument>-classpath</argument>
            <classpath/>
            <argument>lean4j.Lean4JSpike</argument>
          </arguments>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
